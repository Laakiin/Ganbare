<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.lkn.ganbare">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ganbare">
        <receiver
            android:name=".widgets.NextEventWidgetProvider"
            android:label="@string/widget_next_event_label"
            android:exported="true">

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_event_widget_info" />
        </receiver>


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>


        <!-- Widget : tâche prioritaire (NOUVEAU) -->
        <receiver
            android:name="fr.lkn.ganbare.widgets.TopPriorityTaskWidgetProvider"
            android:exported="true"
            android:label="@string/widget_top_task_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/top_task_widget_info" />
        </receiver>
        <!-- Initialise le miroir Room <-> JSON dès le démarrage -->
        <provider
            android:name="fr.lkn.ganbare.core.tasks.sync.TaskMirrorInitProvider"
            android:authorities="${applicationId}.taskmirrorinit"
            android:exported="false"
            android:initOrder="100" />




    </application>
</manifest>
